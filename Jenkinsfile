// Jenkinsfile

pipeline {
    agent any
    
    stages {
        stage('Notify Example') {
            steps {
                script {
                    CommonUtils.notify('Pipeline executed successfully!')
                }
            }
        }
    }
}
